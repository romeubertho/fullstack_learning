import React from "react";

export default function Lesson05() {
    return (
        // Check lesson04 for some react fragment details
        // But remember! JSX elements must be wrapped in an enclosing tag. Fragment is the way to go!
        <React.Fragment>
            <span>Hello</span><span> Fragment</span>
        </React.Fragment>

    )
}